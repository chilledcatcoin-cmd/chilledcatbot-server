/**
 * =====================================================
 * ChilledCatBot - Contests Module Entry
 * =====================================================
 */

const { contests, startContest, endContest, setupContests } = require("./contests");

module.exports = { contests, startContest, endContest, setupContests };
